# UI layer

- `dashboard/` — HUD/command-center components.
- `theme/` — Compose theme primitives.
- Additional frozen UI modules can be split into `watchlist/`, `orders/`, `positions/` and `ai/` without changing the app architecture.
